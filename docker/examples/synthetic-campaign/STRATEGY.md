# Synthetic campaign - STRATEGY.md

Baseline reference (same-window): see agr_logs/state.json after each iteration.

Candidate optimizations (pick ONE per iteration):
- Tune the synthetic sleep durations.
- Nothing else to optimize here - this campaign exists to validate the
  agr-docker pipeline end to end.

Accepted experiments are logged in results.tsv with the commit that produced
them. Keep rows honest: raw numbers, no cherry-picking.
